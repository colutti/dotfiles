#!/usr/bin/env bash
set -euo pipefail

script_path="$(readlink -f -- "${BASH_SOURCE[0]}")"
repo_root="$(cd -- "$(dirname -- "${script_path}")/.." && pwd)"

exec uwsm app -- alacritty -e sh -lc \
  "less -R '${repo_root}/docs/shortcuts.md'"
