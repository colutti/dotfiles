input {
    kb_layout = es
    kb_options = caps:escape
    
    follow_mouse = 1
    sensitivity = 0
    
    # Configurações específicas para mouse (desktop)
    mouse_refocus = false
}

device {
    name = epic-mouse-v1
    sensitivity = -0.5
    accel_profile = flat
}